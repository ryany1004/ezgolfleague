<% if score.course_hole.hole_number == 1 && score.scorecard.tournament.game_type.selected_scorecard_for_score(score) != score.scorecard && score.strokes != 0 -%>
  <%= link_to "Mark Best", "#", {"data-toggle" => "modal", "data-target" => "#ShambleGroup#{scorecards.first.id}"} %>
<% end -%>