/* eslint no-console:0 */

import Vue from 'vue/dist/vue.esm.js';

document.addEventListener('DOMContentLoaded', () => {
  const app = new Vue({
    el: '#tournament-wizard'
  })

  console.log(app)
})